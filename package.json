{
  "name": "bugtracker",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run setup:client && npm run setup:server",
    "setup:client": "cd bugtracker.client && npm i",
    "setup:server": "cd bugtracker.server && npm i",
    "start": "node bugtracker.server/index.js"
  },
  "engines": {
    "node": "^14.15.4"
  }
}
